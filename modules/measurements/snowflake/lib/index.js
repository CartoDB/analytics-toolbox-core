import { version }  from '../package.json';
import { featureCollection, feature, distanceWeight } from '@turf/turf';

export default {
    featureCollection,
    feature,
    distanceWeight,
    version
};