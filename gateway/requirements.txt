# Core dependencies for Analytics Toolbox Gateway

# YAML parsing
PyYAML>=6.0

# JSON schema validation
jsonschema>=4.17.0

# CLI framework
click>=8.1.0

# Progress bars (same as clouds for consistent visuals)
tqdm>=4.65.0

# AWS SDK
boto3>=1.26.0
botocore>=1.29.0

# HTTP requests (for testing)
requests>=2.28.0

# Environment variables
python-dotenv>=1.0.0

# Redshift direct connection (required for deployment and integration tests)
redshift-connector>=2.0.0

# Numerical computing
# Note: Pinned to exact version for consistency with function requirements
numpy==1.26.4
