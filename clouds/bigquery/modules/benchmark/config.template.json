{
    "_README": "Per-function benchmark cases. Copy this file to config.json (gitignored) and edit the source tables / parameter values for your environment. Each function entry is a list of cases. Each case provides values for the ${name} placeholders in the corresponding benchmark file. Example tables are illustrative — point them at real tables you have in your project.",

    "H3_KRING": [
        {"source_table": "myproject.mydataset.h3_table", "h3_column": "h3", "size": 2}
    ],
    "H3_POLYFILL": [
        {"source_table": "myproject.mydataset.polygons_table", "geom_column": "geom", "resolution": 8}
    ],
    "H3_POLYFILL_TABLE": [
        {"input_query": "SELECT geom FROM `myproject.mydataset.polygons_table`", "resolution": 8, "mode": "center", "output_table": "myproject.mydataset.bench_h3_polyfill_center"}
    ]
}
