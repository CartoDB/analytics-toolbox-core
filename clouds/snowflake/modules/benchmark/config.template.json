{
    "_README": "Per-function benchmark cases. Copy this file to config.json (gitignored) and edit the source tables / parameter values for your environment. Each function entry is a list of cases. Each case provides values for the ${name} placeholders in the corresponding benchmark file.",

    "H3_KRING": [
        {"source_table": "MYDB.MYSCHEMA.H3_TABLE", "h3_column": "H3", "size": 2}
    ],
    "H3_POLYFILL": [
        {"source_table": "MYDB.MYSCHEMA.POLYGONS", "geom_column": "GEOM", "resolution": 8}
    ]
}
